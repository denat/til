# Use `Ctrl + R` to search backward in history

In PowerShell, you can reverse command search by pressing Ctrl + R and then typing part of a previously entered command:

```powershell
❯ npm install --save-dev ts-jest
bck-i-search: jes_
```